
This program takes in a sudoku puzzle as a text file, solves it, and prints out the result. If there are more than one puzzles to solve, it solves each puzzle in turn.

What makes this program unique is that there is every operation in the code that might use a for or while loop uses recursion instead.
The entire program uses recursive logic instead of loops.
